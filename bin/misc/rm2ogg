#!/bin/sh
# convert Real Media (RM) files to OGG

for i in "$@"
do
    echo ":: convert $i to OGG"
    NAME=$(basename "$i" .rm)
    mplayer -quiet -vo null -vc dummy -ao pcm:file="$NAME.wav" "$i"
    oggenc "$NAME.wav"
done
