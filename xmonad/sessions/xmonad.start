#!/bin/sh
# startup file for Xmonad

#$HOME=/home/simon

echo "loading xmonad for $HOME ..."

. $HOME/.xprofile

AUTOSTART="${XDG_CONFIG_HOME:-"$HOME/.xmonad"}/autostart.sh"

if test -r $AUTOSTART; then
   echo "loading $AUTOSTART ..."
    . $AUTOSTART
fi

exec xmonad
